<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TaskTaking App</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none" type="text" placeholder="Title Goes Here" name="title">

                <textarea class="block px-5 py-2 bg-zinc-800 resize-none mt-2 w-full rounded-md outline-none" placeholder="Enter your TaskDetails Here..." name="details"></textarea>

                <input class="mt-2 px-5 py-2 bg-blue-500 rounded-md" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap"> 

            <% if(files.length>0){%>
                <% files.forEach(function(val){%>
                    <div class="bg-zinc-800 min-w-72 px-3 py-4 rounded-md">
                        <h1 class="text-white text-2xl tracking-tighter"><%= val%></h1>
                        <div class="flex mt-3 justify-between w-full item-center">
                            <a class="text-blue-300 inline-block " href="/file/<%= val %>">Read More...</a>
                        <a class="text-yellow-600" href="/edit/<%= val%>">Edit FileName</a>
                        <a  class="text-red-600" href="/delete/<%= val %>">Delete</a>
                        </div>
                        
                    </div>
                    <%}) %>
            <%} else{%>
                <h3 class="text-zinc-400">
                    No Task Yet.
                </h3>
            <%}%>

        </div>
    </div>
</body>
</html>